# TODO: Make All Table Cells Editable

## Steps to Complete

- [x] Add Zod validation schemas for Product and ServiceLine types
- [x] Add React state to hold editable data (useState for data array)
- [x] Create EditableCell components for different field types (text, number, select, date)
- [x] Update column definitions to render editable inputs instead of static text
- [x] Add logic to handle cell editing (onBlur, onChange) and update state
- [x] Recalculate group totals when product values change
- [x] Ensure hierarchical data updates propagate correctly (groups, products, services)
